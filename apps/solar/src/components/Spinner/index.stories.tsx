import React from 'react'
import { Spinner } from '.'

export default {
  title: 'Components/Feedback/Spinner',
}

export const Basic = () => <Spinner />
export const WithSize = () => <Spinner size={48} />
